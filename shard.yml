name: deploy-cr
version: 0.1.0

authors:
  - Richard Böhme <richard.boehme1999@gmail.com>

crystal: 0.36.1

license: MIT

targets:
  deploy:
    main: src/deploy-cr/cli.cr

scripts:
  # FIXME: Is this even the proper way to build the binary?
  # We need to use --production here to avoid installing the development dependencies
  postinstall: shards build --production deploy

executables:
  - deploy

dependencies:
  hathor-operation:
    github: ikaru5/hathor-operation
    version: ~> 0.3.0
  teeplate:
    github: mosop/teeplate
  clip:
    github: erdnaxeli/clip
